apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "relayer.fullname" . }}-test-connection"
  labels:
    {{- include "relayer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "relayer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
